# AWS Operations MCP Server Configuration

# Default AWS Region
AWS_REGION=us-east-1

# MCP Authentication Token (required for API access)
MCP_AUTH_TOKEN=your-secret-token-here

# =============================================================================
# Pre-configured Account Role ARNs (Optional)
# =============================================================================
# These allow using account names instead of full role ARNs in tool calls
# Format: ACCOUNT_{NAME}_ROLE_ARN=arn:aws:iam::{ACCOUNT_ID}:role/{ROLE_NAME}

# Example:
# ACCOUNT_PRODUCTION_ROLE_ARN=arn:aws:iam::111111111111:role/aws-ops-target
# ACCOUNT_STAGING_ROLE_ARN=arn:aws:iam::222222222222:role/aws-ops-target
# ACCOUNT_DEVELOPMENT_ROLE_ARN=arn:aws:iam::333333333333:role/aws-ops-target

# =============================================================================
# Pre-configured Account Profiles (For Local Development)
# =============================================================================
# These allow using AWS profiles from ~/.aws/credentials in tool calls
# Format: ACCOUNT_{NAME}_PROFILE=profile-name
# Ideal for local development with multiple AWS accounts

# Example:
# ACCOUNT_PRODUCTION_PROFILE=prod
# ACCOUNT_STAGING_PROFILE=staging
# ACCOUNT_DEVELOPMENT_PROFILE=dev

# =============================================================================
# Local Development Only (Not for production)
# =============================================================================
# For local development, you can also provide AWS credentials directly
# In production, use IRSA (IAM Roles for Service Accounts)

# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=

# Or use AWS_PROFILE for default profile
# AWS_PROFILE=default
