# AWS Operations MCP Server Configuration

# Default AWS Region
AWS_REGION=us-east-1

# MCP Authentication Token (required for API access)
MCP_AUTH_TOKEN=your-secret-token-here

# =============================================================================
# AWS Profile (Local Development)
# =============================================================================
# Default AWS profile from ~/.aws/credentials
# This is used when no 'profile' parameter is passed in tool calls
AWS_PROFILE=default

# You can switch between companies/accounts using the 'profile' parameter:
#   {"secret_id": "db", "profile": "igual-prod"}
#   {"secret_id": "db", "profile": "maistodos-staging"}
#
# Your ~/.aws/credentials should have profiles like:
#   [igual-prod]
#   [igual-staging]
#   [maistodos-prod]
#   [maistodos-staging]

# =============================================================================
# Role ARNs (Production - AssumeRole)
# =============================================================================
# For production with IRSA, use role_arn parameter instead of profile:
#   {"secret_id": "db", "role_arn": "arn:aws:iam::111111111111:role/aws-ops-target"}
#
# Optional: Pre-configure role ARNs for easier reference
# ACCOUNT_PRODUCTION_ROLE_ARN=arn:aws:iam::111111111111:role/aws-ops-target
# ACCOUNT_STAGING_ROLE_ARN=arn:aws:iam::222222222222:role/aws-ops-target
